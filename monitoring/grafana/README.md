# grafana

Grafana provisioning root for this project.

## Purpose

- Auto-load datasource configuration.
- Auto-load dashboard definitions at startup.

This keeps dashboards reproducible: a fresh environment starts with the same monitoring layout without manual setup in the Grafana UI.
