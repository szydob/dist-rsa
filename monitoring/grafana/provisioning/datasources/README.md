# datasources

Grafana datasource provisioning files.

## Files

- `prometheus.yml`: defines Prometheus as the default datasource.

## Why this matters

Datasource provisioning removes manual Grafana setup and ensures dashboards are connected to the expected Prometheus endpoint immediately after startup.
