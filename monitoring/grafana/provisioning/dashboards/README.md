# dashboards

Dashboard provisioning configuration and dashboard JSON definitions.

## Files

- `dashboards.yml`: tells Grafana where dashboard JSON files are located.
- `agent-pool-dashboard.json`: prebuilt dashboard for Agent Pool metrics.

## Dashboard focus

The included dashboard is optimized to visualize short utilization spikes and agent distribution during factorization tasks.
