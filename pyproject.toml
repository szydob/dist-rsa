[project]
name = "ray-rsa"
version = "0.1.0"
description = "Distributed RSA encryption/decryption with Ray"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "ray[default]>=2.20,<3.0",
  "streamlit>=1.43,<2.0",
]