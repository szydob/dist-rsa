services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    shm_size: "2.5gb"
    ports:
      - "8501:8501"
    volumes:
      - ./src:/app/src
    environment:
      - RAY_ACCEL_ENV_VAR_OVERRIDE_ON_ZERO=0
    restart: unless-stopped